import React from "react";

export default class Login extends React.PureComponent {
    render() {
        return (
            <React.Fragment>
                <h1>Login</h1>
            </React.Fragment>
        )
    }
}